@model PPOK_System.Models.Message

@{
    ViewBag.Title = "History";
    List<PPOK_System.Models.Prescription> prescription = new List<PPOK_System.Models.Prescription>();
    List<PPOK_System.Models.Person> person = new List<PPOK_System.Models.Person>();
    PPOK_System.Service.Database db = new PPOK_System.Service.Database();
    PPOK_System.Models.Message message = new PPOK_System.Models.Message();

    DateTime today = DateTime.UtcNow.Date;
    String mmddyy = today.ToString("MM/dd/yy");

    person = db.ReadAllPersons();
    
}
}

<style>
    body {
        margin-top: 2%;
    }

    center {
        margin: auto;
    }
</style>


<script src="http://ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>
<script src="http://getbootstrap.com/dist/js/bootstrap.min.js"></script>

<div class="form-group">
    <label class="col-md-3 control-label" for="searchInput"></label>
    <div class="col-md-3">
        <span><input id="searchInput" name="searchInput" type="search" placeholder="Search...." class="form-control input-md"></span>
      

    </div>
</div>
<div></div>
<div class="container" style='margin-top:75px'>
    <div class="row">


        <div class="col-md-12">
            <h4>Refill History</h4>
            <div class="table-responsive">

                <table id="mytable" class="table table-bordred table-striped">

                    <thead>

                    <th>Name</th>
                    <th>Prescription</th>
                    <th>Date</th>
                    <th>Response</th>

                    </thead>
                    <tbody>
                        @{
                            foreach (PPOK_System.Models.Person p in person)
                            {
                                prescription = db.ReadAllPrescriptionsForPerson(p.person_id);
                                //message = db.ReadSingleMessage(p.person_id);                  //Needs fixed to get actuial message.
                                foreach (PPOK_System.Models.Prescription pr in prescription)
                                {
                                    // @message = db.ReadSingleMessage(pr.person_id);
                                    <tr>
                                        <td>@p.first_name @p.last_name</td>
                                        <td>@pr.drug.drug_name</td>
                                        <td>@message.fill_date.ToString("MM/dd/yy")</td>
                                        <td>@message.response</td>
                                    </tr>
                                }

                            }
                        }
                        

                    </tbody>

                </table>
            </div>

        </div>
    </div>
</div>

<script>
    $(document).ready(function () {
        $("#mytable #checkall").click(function () {
            if ($("#mytable #checkall").is(':checked')) {
                $("#mytable input[type=checkbox]").each(function () {
                    $(this).prop("checked", true);
                });

            } else {
                $("#mytable input[type=checkbox]").each(function () {
                    $(this).prop("checked", false);
                });
            }
        });

        $("[data-toggle=tooltip]").tooltip();
    });


</script>